import AdminPlaceholder from './AdminPlaceholder'

export default function SalesPage() {
  return (
    <AdminPlaceholder
      title="Sales"
      description="An orders table with Excel export via xlsx (SheetJS) arrives in Phase 5."
    />
  )
}
