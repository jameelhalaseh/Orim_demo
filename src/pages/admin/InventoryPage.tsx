import AdminPlaceholder from './AdminPlaceholder'

export default function InventoryPage() {
  return (
    <AdminPlaceholder
      title="Inventory"
      description="A category table with derived stock and low-stock flags, add/adjust item, and the movement-history view arrive in Phase 5. Stock is always derived from the movement ledger."
    />
  )
}
